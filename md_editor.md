```markdown
# Markdown 笔记应用开发流程

## 项目概述
实现一个完整的 Markdown 笔记应用。用户可以创建笔记，输入标题和内容。界面采用左右分栏：左侧为编辑器与笔记列表，右侧为实时预览渲染效果。支持笔记搜索、删除，数据持久化到 LocalStorage，并对代码块进行语法高亮，提升专业感。

## 技术选型
- **前端框架**：React + TypeScript + Vite  
- **Markdown 解析**：react-markdown  
- **代码高亮**：react-syntax-highlighter  
- **样式方案**：Tailwind CSS  
- **数据存储**：LocalStorage  

## 功能特性
- 左右分栏布局（PC端左右，移动端上下）
- 左侧：笔记列表 + 搜索 + Markdown 编辑器
- 右侧：实时渲染的 HTML 预览
- 代码块语法高亮（支持多语言）
- 笔记管理：新建、切换、删除、搜索（按标题）
- 数据自动保存至 LocalStorage

---

## 开发步骤

### 1. 实现基础布局
第一步是搭建应用骨架，创建左右分栏结构。使用 Tailwind CSS 的 Flex 或 Grid 可快速实现响应式布局，桌面端左右并排，移动端自动切换为上下布局。

```text
创建左右分栏布局，左侧是Markdown编辑器(大文本框)，右侧是预览区。使用Tailwind CSS实现响应式布局。
```
生成一个美观的分栏页面，左侧放置编辑器与笔记管理区域，右侧作为预览区。

### 2. 集成 Markdown 解析
引入 `react-markdown` 库，将 Markdown 文本转换为 HTML 并在预览区渲染。只需将文本传入组件即可自动完成解析。

```text
使用 react-markdown 将 Markdown 文本转换为HTML，在预览区显示渲染后的效果。
```
这一步完成后，右侧预览区已经能够展示静态的 Markdown 内容。

### 3. 实现实时预览
监听编辑器内容变化，将文本存入 React 状态，并同步传递给预览组件。用户每输入一个字符，右侧预览区都会即时更新渲染结果。

```text
监听编辑器的输入变化，实时更新预览区。用户在左侧编辑时，右侧同步显示Markdown渲染后的效果。
```
借助 React 的状态管理，将编辑器 `textarea` 的值绑定到 state，同时将该 state 作为 `react-markdown` 的输入，实现毫秒级实时预览。

### 4. 添加代码高亮
为代码块配置 `react-syntax-highlighter`，使各种编程语言的代码块呈现专业的语法高亮效果。

```text
配置 react-syntax-highlighter，让代码块支持语法高亮。支持多种编程语言，比如 JavaScript、Python、Java 等。
```
这会让预览中的代码块自动检测语言并应用对应的高亮样式。

### 5. 添加笔记管理功能
一次性实现完整的笔记管理模块，包括列表展示、新建、切换、删除、搜索以及 LocalStorage 持久化。

```text
实现笔记管理功能:
- 左侧添加笔记列表，显示所有笔记的标题
- 点击笔记可以切换到该笔记
- 支持创建新笔记、删除笔记
- 支持搜索笔记(按标题搜索)
- 数据保存在LocalStorage
```
根据这些要求创建笔记增删改查的逻辑，并自动处理好数据持久化。

---

## 关键技术点

- **实时预览**：使用 `react-markdown` 将编辑器内容实时转换为 HTML，配合 React 状态驱动视图更新。
- **代码高亮**：利用 `react-syntax-highlighter` 为代码块添加多语言语法高亮，提升阅读体验。
- **用户体验优化**：可进一步添加 Tab 键插入空格（而非切换焦点）、Ctrl+B 加粗等快捷键支持，以及自动保存草稿等功能。

## 扩展思路
基础功能完善后，可以继续加入以下高级特性：
- 图片上传：支持在笔记中插入图片
- 目录导航：自动解析标题生成文章大纲
- 导出 PDF：将笔记导出为 PDF 文件
- 主题切换：提供多种编辑器主题（暗色/亮色）
- Markdown 风格扩展：支持 GitHub 风格、标准风格等不同渲染样式

按照以上流程，即可逐步构建一个功能完整、体验优秀的 Markdown 笔记应用。
